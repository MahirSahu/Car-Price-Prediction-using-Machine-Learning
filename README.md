# Car-Price-Prediction-using-Machine-Learning
A machine learning project that predicts the resale price of used cars using Linear Regression. The project includes data cleaning, preprocessing, one-hot encoding of categorical variables, model training, automatic selection of the best random state based on R² score, and model serialization using Pickle for future predictions.
